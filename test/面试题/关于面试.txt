到底什么是Python？你可以在回答中与其他技术进行对比。
下面是一些关键点：
　　Python是一种解释型语言。这就是说，与C语言和C的衍生语言不同，Python代码在运行之前不需要编译。其他解释型语言还包括PHP和Ruby。
　　Python是动态类型语言，指的是你在声明变量时，不需要说明变量的类型。你可以直接编写类似x=111和x="I'm a string"这样的代码，程序不会报错。
　　Python非常适合面向对象的编程（OOP），因为它支持通过组合（composition）与继承（inheritance）的方式定义类（class）。Python中没有访问说明符（access specifier，类似C++中的public和private），这么设计的依据是“大家都是成年人了”。
　　在Python语言中，函数是第一类对象（first-class objects）。这指的是它们可以被指定给变量，函数既能返回函数类型，也可以接受函数作为输入。类（class）也是第一类对象。
　　Python代码编写快，但是运行速度比编译语言通常要慢。好在Python允许加入基于C语言编写的扩展，因此我们能够优化代码，消除瓶颈，这点通常是可以实现的。numpy就是一个很好地例子，它的运行速度真的非常快，因为很多算术运算其实并不是通过Python实现的。
　　Python用途非常广泛——网络应用，自动化，科学建模，大数据应用，等等。它也常被用作“胶水语言”，帮助其他语言和组件改善运行状况。
　　Python让困难的事情变得容易，因此程序员可以专注于算法和数据结构的设计，而不用处理底层的细节。
　　
注意： 如果你应聘的是一个Python开发岗位，你就应该知道这是门什么样的语言，以及它哪里好哪里不好。

Python和多线程（multi-threading）到底好不好？？？
　　Python并不支持真正意义上的多线程。Python中提供了多线程包，但是如果你想通过多线程提高代码的速度，使用多线程包并不是个好主意。Python中有一个被称为Global Interpreter Lock（GIL）的东西，它会确保任何时候你的多个线程中，只有一个被执行。线程的执行速度非常之快，会让你误以为线程是并行执行的，但是实际上都是轮流执行。经过GIL这一道关卡处理，会增加执行的开销。这意味着，如果你想提高代码的运行速度，使用threading包并不是一个很好的方法。但是如果你想同时执行一些任务，而且不考虑效率问题，那么使用这个包是完全没问题的，而且也很方便
----------------------------------------------------------------------------
下面代码会输出什么：
def f(x,l=[]):
    for i in range(x):
        l.append(i*i)
    print (l)
f(2)
f(3,[3,2,1])
f(3)
答案：
	    [0, 1]
　　　　　　　[3, 2, 1, 0, 1, 4]
　　　　　　　[0, 1, 0, 1, 4]  注意：第三次函数调用沿用了之前的旧列表
----------------------------------------------------------------------------
说一下这两个参数的意思：*args，**kwargs
　　答：如果我们不确定要往函数中传入多少个参数，或者我们想往函数中以列表和元组的形式传参数时，那就使要用*args；如果我们不知道要往函数中传入多少个关键词参数，或者想传入字典的值作为关键词参数时，那就要使用**kwargs。
----------------------------------------------------------------------------
补充  装饰器：
　　装饰器是一种特殊的函数，要么接受函数作为输入参数，并返回一个函数，要么接受一个类作为输入参数，并返回一个类。@标记语法（syntactic sugar），可以让你以简单易读得方式装饰目标对象
----------------------------------------------------------------------------
　　你失败过吗？
　　这种问题在回答的时候要体现出你虽然失败过但是勇于承担错误并且为错误负责，以及从错误中汲取经验，不要说自己NB的错来没有失败过。
----------------------------------------------------------------------------
　　你简历上的项目都是你做的吗？
　　千万不要说不是，上个月给04班做模拟面试，我问一个同学同样的问题，他想都没想就说不是，GG了。。。  不是你做的那你写在简历上是干嘛的？小学作文要求凑够八百字吗。。。
　　所以说 咱们中期项目答辩分组的目的很明确了，大家一起完成，虽然肯定会有打酱油的，所以当咱们的项目完成的时候，希望大家在组内多聊一下，谁做了哪一个模块怎么做的代码为什么这么写。目的是为了能让每个人把项目完整的讲出来。出去面试最主要的就是讲你的项目，项目最能体现你的能力和为公司带来多大的价值。对于基础部分，没有开发经验的人通常都会被要求做面试题。有过开发经验的可能会直接和技术部门对接开始聊自己做过的项目。
　　面试官怎么能判断出这个项目是不是你做的？
　　你说是就是呗，他还能监视你咋地，所以他就会让你聊一聊你的项目，比如他会问你 你的项目是c/s架构还是b/s架构  为什么用这种架构，你的服务器问什么用多进程，它和多线程有什么区别 哪个好。。等等。所以 咱们项目答辩完之后 组内一定要多沟通，争取吃透自己的项目。
　　第一印象很重要，见你的第一面，第一声嗓门，都是HR衡量你这个人的依据。
　　为什么让大家早晨讲PPT，还是内容不限的那种，真的不需要你讲出什么花儿来，就是锻炼一下你的语言表达能力和声线。第一印象真的很重要，谁都喜欢自信的人。